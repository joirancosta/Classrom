-- Esse é um comentário

CREATE TABLE products (
	id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  price REAL NOT NULL,
  category TEXT NULL DEFAULT 'general'
)

ALTER TABLE products ADD quantity INTEGER NOT NULL

ALTER TABLE products DROP COLUMN quantity

ALTER TABLE products RENAME COLUMN name TO description

ALTER TABLE products RENAME COLUMN description TO name

ALTER TABLE products RENAME TO items

ALTER TABLE items RENAME TO products

DROP TABLE products

INSERT INTO products (name, price) VALUES ('Mouse', 50)

INSERT INTO products (name, price, category) VALUES ('Teclado', 500.50, 'accessory')

SELECT * FROM sqlite_sequence

-- Seleciona os registros
SELECT * FROM products
-- Seleciona colunas especificas
SELECT name FROM products
SELECT name, price FROM products
-- Definindo a ordem de exibição da coluna
SELECT price, name FROM products

UPDATE products SET price = 45.90, category = 'accessory'

UPDATE products SET price = 45.90 WHERE id = 1

INSERT INTO products (name, price, category) VALUES ('Microfone', 550, 'audio');
INSERT INTO products (name, price, category) VALUES ('Webcam', 1200, 'image');
INSERT INTO products (name, price, category) VALUES ('Headseat', 800, 'audio');

DELETE FROM products WHERE id = 3

SELECT id, name FROM products
SELECT id, category FROM products
SELECT * FROM products WHERE name = 'Mouse'
SELECT * FROM products WHERE price = 1200
SELECT * FROM products WHERE name LIKE '%cam'
SELECT * FROM products WHERE name LIKE 'web%'
SELECT * FROM products WHERE name LIKE '%eb%'

-- Seleciona valores diferentes.
SELECT * FROM products WHERE price <> 800
-- Seleciona valores maiores que a condição.
SELECT * FROM products WHERE price > 505
-- Seleciona valores menores que a condição.
SELECT * FROM products WHERE price < 505
-- Seleciona valores maiores ou iguais que a condição.
SELECT * FROM products WHERE price >= 500.5
-- Seleciona valores menores ou iguais que a condição.
SELECT * FROM products WHERE price <= 800
-- O operador AND retorna os registros que atende todos os criterios (TRUE)
SELECT * FROM products WHERE price > 500 AND price < 1000 AND id > 2
-- O operador OR retorna os registros que atende pelo menos um dos criterios
SELECT * FROM products WHERE price < 500 OR price > 1000
-- Operadores AND e OR juntos
SELECT * FROM products WHERE price > 45 AND price < 1000 AND category = 'audio' OR category = 'image'
SELECT * FROM products WHERE (price > 45 AND price < 1000) AND (category = 'audio' OR category = 'image')

SELECT * FROM products WHERE price >= 600 AND price <= 1200
SELECT * FROM products WHERE price BETWEEN 600 AND 1200

-- Seleciona multiplos valores númericos
SELECT * FROM products WHERE price IN (550, 800)
-- Seleciona multiplos valores do tipo texto
SELECT * FROM products WHERE category IN ('audio', 'image')
-- ACS (crescente) - padrão
SELECT * FROM products ORDER BY price
SELECT * FROM products ORDER BY price ASC
SELECT * FROM products ORDER BY name
-- DESC (decrescente)
SELECT * FROM products ORDER BY price DESC
SELECT * FROM products ORDER BY name DESC

-- WHERE + ORDER BY
SELECT * FROM products WHERE category = 'audio' ORDER BY price ASC

SELECT * FROM products ORDER BY price DESC LIMIT 3

SELECT * FROM products

SELECT COUNT(*) FROM products

SELECT COUNT(name) FROM products

SELECT COUNT(price) FROM products

SELECT COUNT(*) FROM products WHERE price >= 600

SELECT SUM(price) FROM products

SELECT SUM(price) FROM products WHERE category = 'audio'

SELECT AVG(price) FROM products WHERE category = 'audio'

SELECT COUNT(*) AS TOTAL FROM products
SELECT COUNT(*) AS 'TOTAL' FROM products
-- O AS é opcional
SELECT COUNT(*) TOTAL FROM products
SELECT COUNT(*) 'TOTAL' FROM products]
-- Nome composto
SELECT COUNT(*) AS 'TOTAL DE PRODUTOS' FROM products
SELECT COUNT(*) AS [TOTAL DE PRODUTOS] FROM products

SELECT id AS code, name AS 'Nome do produto', price preco FROM products

SELECT name, COUNT(*) FROM products 
SELECT category, COUNT(*) AS total FROM products GROUP BY category ORDER BY total DESC

SELECT category, COUNT(*) AS total 
FROM products 
WHERE price > 600
GROUP BY category
ORDER BY total DESC